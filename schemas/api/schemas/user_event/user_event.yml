title: "UserEventModel"
type: object
description: User event model , ties user to the event
required: [id, user_id, event_id, registered, paid, enabled]
properties:
  id:
    description: ID of user_event
    type: number
  user_id:
    description: ID of user
    type: number
  event_id:
    description: ID of event
    type: number
  registered:
    description: Epoch registered
    type: string
  paid:
    type: boolean
    description: User has paid or not
  receipt:
    type: string
    description: Reciept copy
  enabled:
    type: boolean
    description: Is user still enrolled
